name: CI

on:
  push:
    branches: [main, feat/**]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Toggle this to 'true' when deploying and ready to run E2E on CI.
    # Alternatively, override via 'env' at workflow_dispatch or environment.
    env:
      ENABLE_E2E: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node version from .nvmrc
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Verify Tools Installation
        run: |
          npx biome --version
          npx tsc --version
          echo "Node.js version: $(node --version)"
          echo "npm version: $(npm --version)"

      - name: Type Check
        run: npx tsc --noEmit

      - name: Lint
        run: npx biome lint .

      - name: Unit Tests
        run: npm run test:unit -- --run

      - name: Full Flow Integration (splitOnly)
        run: npm run test:full-flow --if-present

      - name: Start App (background) for E2E
        if: ${{ env.ENABLE_E2E == 'true' }}
        run: |
          nohup npm run dev &
          echo $! > dev.pid
          n=0; until curl -sf http://localhost:3000/api/health || [ $n -ge 40 ]; do n=$((n+1)); sleep 3; done
        shell: bash

      - name: Playwright Install Browsers
        if: ${{ env.ENABLE_E2E == 'true' }}
        run: npx playwright install --with-deps

      - name: E2E Tests (health)
        if: ${{ env.ENABLE_E2E == 'true' }}
        env:
          E2E_BASE_URL: http://localhost:3000
        run: npm run test:e2e -- --grep "health endpoint"

      - name: Upload Test Results (optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          if-no-files-found: ignore
          path: |
            test-results
            playwright-report
            coverage

      - name: Stop Dev Server
        if: ${{ always() && env.ENABLE_E2E == 'true' }}
        run: |
          if [ -f dev.pid ]; then kill $(cat dev.pid) || true; fi
        shell: bash
