// Legacy script adapters removed - new prompt format only
// This file is kept for potential future utility functions but current content removed to prevent context pollution

// If new format conversion utilities are needed in the future, they should be added here
// with clear documentation about their purpose and relation to the current prompt format

export function placeholder() {
  // This is a placeholder to keep the file structure
  // Remove this when actual utilities are added
}
