/// <reference types="vitest" />

// Keep this file minimal. Avoid declaring a wildcard '@/ *' module because
// that would shadow real source modules (and make many real imports `unknown`).
// If additional non-code assets need declarations (images, css), add them
// explicitly in this file.
