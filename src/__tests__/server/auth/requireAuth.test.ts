/**
 * Deprecated deep auth tests (moved to centralized test folder)
 * Kept as a skipped placeholder to preserve intent but avoid running in unit suites.
 */

import { describe, it } from 'vitest'

// Placeholder to avoid duplicate tests running in deep folder.
describe.skip('Deprecated deep auth tests - skipped', () => {
  it('noop', () => {
    // no-op
  })
})
