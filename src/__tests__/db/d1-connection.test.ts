import { describe } from 'vitest'

// D1 removed from the codebase; skip D1 compatibility test.
describe.skip('D1 compatibility tests (removed)', () => {
  // Removed per migration to Node + SQLite. Retain for historical reference only.
})
