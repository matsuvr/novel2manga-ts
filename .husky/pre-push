#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run full checks before pushing

echo "Running typecheck..."
npm run -s typecheck || exit 1

echo "Running Biome check..."
npm run -s check:ci || exit 1

echo "Running tests..."
npm test || exit 1
